{
  "name": "root",
  "private": true,
  "scripts": {
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "husky": "^3.0.4",
    "lerna": "^3.10.7",
    "lint-staged": "^8.1.0"
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx,graphql}": [
      "prettier --write",
      "git add"
    ]
  }
}
