{
  "name": "root",
  "private": true,
  "scripts": {
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "lerna": "^3.0.0-rc.0",
    "husky": "^1.1.3",
    "lint-staged": "^7.2.0"
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx,graphql}": [
      "prettier --write",
      "git add"
    ]
  }
}
