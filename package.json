{
  "name": "root",
  "private": true,
  "scripts": {
    "precommit": "lint-staged"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lerna": "^3.10.7",
    "lint-staged": "^8.1.0"
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx,graphql}": [
      "prettier --write",
      "git add"
    ]
  }
}
