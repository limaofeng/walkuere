{
  "name": "root",
  "private": true,
  "scripts": {
    "precommit": "lint-staged"
  },
  "devDependencies": {
    "lerna": "^3.0.0-rc.0",
    "husky": "^0.14.3",
    "lint-staged": "^8.0.5"
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx,graphql}": [
      "prettier --write",
      "git add"
    ]
  }
}
